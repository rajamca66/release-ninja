{
  "name": "frontend-builder",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && bundle install",
    "assets:build": "cd frontend && gulp build:production",
    "assets:watch": "cd frontend && gulp watch"
  },
  "engines": {
    "node": "4.3.2"
  }
}
