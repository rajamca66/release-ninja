{
  "name": "frontend-builder",
  "version": "0.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && gem install bundler && bundle install && gulp build:production"
  },
  "engines": {
    "node": "4.3.2"
  }
}
