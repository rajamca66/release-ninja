{
  "name": "frontend-builder",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install && gem install bundler && bundle install && gulp build"
  },
  "engines": {
    "node": "4.3.2"
  }
}
